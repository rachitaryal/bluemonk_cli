import express from 'express'
// import app_router from '../App/Routes/AppRoutes';
const router_main = express.Router();


//All routes
// router_main.use("/app", app_router);

export default router_main;
